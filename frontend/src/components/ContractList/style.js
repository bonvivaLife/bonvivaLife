import styled from 'styled-components'
import theme from '../../theme'

export const ContractListWrapper = styled.ul`
display: flex;
flex-direction: column;
width: 100%;
list-style: none;
padding: 0;
margin: 0;
border-top: 0.9px solid ${theme.lightGray};

`